# Question
I often run into code like 

net=ResNet()

net(x)

and ResNet looks like I can't insert something into it. But it works.

And this is because of method _ _call_ _

Here, I am going to write about method like that including _ _call_ _

# init
